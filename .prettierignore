# Generated content index — written verbatim by `pnpm run content:index`
# (JSON.stringify(…, 2)). Prettier must NOT reformat it, or it diverges from
# the generator and `content:index:check` (pre-push / CI guard) reports drift
# on every commit that touches the corpus.
content/.index/

# pnpm owns the lockfile's format.
pnpm-lock.yaml

# Build output.
dist/
